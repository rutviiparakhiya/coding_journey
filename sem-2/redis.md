REDIS

redis is a sql database
it is very useful for chacing
it stores data in string data

string - stores text data
object - key value pairs
list - allows to store duplicate values
sets - does not allow to store any duplicate values
hashmap - it stores value in key value pair || but it is more efficient than object because it is stored in memory
